require "spec_helper"

RSpec.describe Pronto::Goodcheck do
  it "has a version number" do
    expect(Pronto::Goodcheck::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
